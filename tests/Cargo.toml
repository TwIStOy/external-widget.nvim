[package]
name = "tests"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
ext-widget-core = { path = "../crates/ext-widget-core" }
nvim-oxi = { version = "0.4.1", features = [
  "neovim-nightly",
  "test",
  "mlua",
  "libuv",
] }
all_asserts = "2.3.1"
